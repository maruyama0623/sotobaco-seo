name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      blog: ${{ steps.filter.outputs.blog }}
      lp: ${{ steps.filter.outputs.lp }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Detect changed sites
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            blog:
              - 'sites/blog/**'
              - 'images/**'
              - 'packages/**'
            lp:
              - 'sites/lp/**'
              - 'images/**'
              - 'packages/**'

  deploy-blog:
    needs: detect-changes
    if: needs.detect-changes.outputs.blog == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: sites/blog/.nvmrc
          cache: npm
          cache-dependency-path: sites/blog/package-lock.json

      - name: Install dependencies
        working-directory: sites/blog
        run: npm ci

      - name: Build
        working-directory: sites/blog
        run: npm run build

      - name: Deploy to production
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SFTP_HOST }}
          port: 8022
          username: ${{ secrets.SFTP_USERNAME }}
          key: ${{ secrets.SFTP_KEY }}
          source: "sites/blog/out/"
          target: ${{ secrets.PRODUCTION_BLOG_PATH }}
          strip_components: 3
          overwrite: true

  deploy-lp:
    needs: detect-changes
    if: needs.detect-changes.outputs.lp == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: sites/lp/.nvmrc
          cache: npm
          cache-dependency-path: sites/lp/package-lock.json

      - name: Install dependencies
        working-directory: sites/lp
        run: npm ci

      - name: Build
        working-directory: sites/lp
        run: npm run build

      - name: Deploy to production
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SFTP_HOST }}
          port: 8022
          username: ${{ secrets.SFTP_USERNAME }}
          key: ${{ secrets.SFTP_KEY }}
          source: "sites/lp/out/"
          target: ${{ secrets.PRODUCTION_LP_PATH }}
          strip_components: 3
          overwrite: true
