#!/bin/bash
# コンテキスト圧縮後に重要ルールを再注入する

cat <<'EOF'
# コンテキスト圧縮後リマインド

以下のルールを引き続き遵守してください:

- **git pull**: docs/*.md 編集前に必ず git pull を実行
- **税抜表示**: 価格はすべて税抜で表示（「税抜」と明記）
- **PII保護**: 個人情報はKVに集約、Slack/ログにPII含めない
- **宛名**: 「会社名＋ご担当者様」（個人名をAIに送信しない）
- **動作確認**: 実装後は Playwright MCP で動作確認
- **Playwright**: MCPツール直接呼び出しのみ、コード実行禁止
- **記録**: 重要情報は docs/*.md とメモリの両方に記録
- **確認**: 判断に迷う場合は社長に確認してから動く
EOF
exit 0
